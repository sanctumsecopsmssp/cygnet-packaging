# Deprecated location

The Homebrew formula lives at `Formula/cygnet-provider.rb` in the repository root.

Homebrew only discovers formulae in the repository root, a `Formula/`
subdirectory, or a `HomebrewFormula/` subdirectory. Files in this `homebrew/`
directory are ignored by `brew`.
